initSidebarItems({"constant":[["VIRTIO0",""]],"fn":[["virtio_dma_alloc","这里用 new_contiguous 可以要求分配一段连续的内存。 因为这个函数是面向 virtio-drivers 的接口，而且仅在内核启动时初始化， 所以这里默认可以拿到需要的空间，不处理分配失败导致的异常"],["virtio_dma_dealloc","Frame 在 Drop 时会释放页帧，所以这里不用做其他处理"],["virtio_phys_to_virt","默认只在内核态才访问块设备，所以直接用内核固定偏移的虚拟地址"],["virtio_virt_to_phys","默认只在内核态才访问块设备，所以直接用内核固定偏移的虚拟地址"]],"static":[["QUEUE_FRAMES",""]],"struct":[["VirtIOBlock",""]]});